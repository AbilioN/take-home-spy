You are inside the existing fullstack project:

- NestJS backend already running
- React Native (Expo + TypeScript) frontend inside abilio-solution/app
- User authentication already implemented
- TheCatAPI integration already exists

Now transform the CatHomeScreen into a Tinder-style swipe experience for cats.

--------------------------------------------------
GOAL
--------------------------------------------------

After user login, instead of showing a list of cats,
show ONE cat profile at a time in a swipeable card (like Tinder).

User can:
- Swipe right → "I want to take care of this cat"
- Swipe left → Skip

When swiped:
- Animate card off screen
- Load a new random cat
- Repeat infinitely

--------------------------------------------------
INSTALL DEPENDENCIES
--------------------------------------------------

Install:

react-native-gesture-handler
react-native-reanimated
react-native-deck-swiper

Configure reanimated properly in babel.config.js.

--------------------------------------------------
CREATE NEW SCREEN
--------------------------------------------------

Rename CatHomeScreen to:

CatTinderScreen.tsx

It must:

1) Fetch random cats from TheCatAPI
2) Transform each cat into a profile object:

{
  id: string
  imageUrl: string
  name: string (random fake name generated locally)
  age: random number between 1-12
  description: random short personality description
}

Generate random cat names from a local array like:
["Whiskers", "Luna", "Milo", "Shadow", "Simba", "Oliver", "Nala", "Chloe"]

Generate random personality traits like:
- "Loves cuddles and naps."
- "Very playful and energetic."
- "Shy but extremely sweet."
- "Loves chasing laser pointers."

--------------------------------------------------
UI REQUIREMENTS
--------------------------------------------------

Center card with:

- Large cat image
- Name + age
- Short description
- Clean modern styling
- Rounded card corners
- Slight shadow

Swipe behavior:

Swipe Right:
- Console.log("Liked:", cat.name)

Swipe Left:
- Console.log("Skipped:", cat.name)

After swipe:
- Automatically load a new cat profile

--------------------------------------------------
STATE MANAGEMENT
--------------------------------------------------

- Use React Query to fetch cat data
- Prefetch next cat to avoid delay
- Handle loading and errors

--------------------------------------------------
NAVIGATION
--------------------------------------------------

After login:
LoginScreen → CatTinderScreen

Remove old CatHomeScreen logic.

--------------------------------------------------
EXTRA
--------------------------------------------------

- Add a small heart animation when swiping right
- Add a small "NOPE" overlay when swiping left
- Keep performance smooth

--------------------------------------------------
FINAL STEP
--------------------------------------------------

Ensure everything runs:

npx expo start

Do not explain what you are doing.
Just implement the Tinder-style cat swipe experience cleanly.