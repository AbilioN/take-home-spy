You are inside the existing Expo + TypeScript mobile app
inside abilio-solution/app.

The app already has:
- User authentication implemented
- Axios client configured
- POST /locations endpoint integrated
- userId stored in AsyncStorage
- Expo Location installed

Now implement REAL background location tracking.

--------------------------------------------------
CRITICAL REQUIREMENT
--------------------------------------------------

The app MUST send the user's location to the backend:

- While app is in foreground
- While app is minimized (background)
- While device screen is locked
- While app is in standby
- Even if the app is closed (killed state), as long as the OS allows background location execution (especially on Android)

This must use proper background task execution,
NOT a simple setInterval.

--------------------------------------------------
INSTALL (if not already installed)
--------------------------------------------------

expo-task-manager
expo-location

--------------------------------------------------
IMPLEMENTATION
--------------------------------------------------

1) Create:

src/services/backgroundLocationService.ts

2) Define background task:

const LOCATION_TASK_NAME = "background-location-task"

Use:

TaskManager.defineTask(LOCATION_TASK_NAME, async ({ data, error }) => {
   ...
})

3) Inside task:

- Retrieve userId from AsyncStorage
- Extract latitude and longitude
- Send POST /locations:

{
  userId: string,
  latitude: number,
  longitude: number
}

Use existing Axios client.

4) Start tracking using:

Location.startLocationUpdatesAsync(LOCATION_TASK_NAME, {
   accuracy: Location.Accuracy.Balanced,
   timeInterval: LOCATION_INTERVAL_MINUTES * 60 * 1000,
   distanceInterval: 0,
   showsBackgroundLocationIndicator: false,
   pausesUpdatesAutomatically: false,
})

5) Define configurable interval:

const LOCATION_INTERVAL_MINUTES = 5

6) Implement:

startBackgroundTracking()
stopBackgroundTracking()

--------------------------------------------------
PERMISSIONS (MANDATORY)
--------------------------------------------------

Request BOTH:

Location.requestForegroundPermissionsAsync()
Location.requestBackgroundPermissionsAsync()

If background permission is denied:
- Show alert explaining that background tracking will not work.

--------------------------------------------------
ANDROID CONFIGURATION
--------------------------------------------------

Ensure:

- Add necessary background location permissions in app.json
- Enable background location in Expo configuration
- Add android.permission.ACCESS_BACKGROUND_LOCATION

--------------------------------------------------
IOS CONFIGURATION
--------------------------------------------------

Add to app.json:

"ios": {
  "infoPlist": {
    "UIBackgroundModes": ["location"],
    "NSLocationAlwaysAndWhenInUseUsageDescription": "We use your location to help you find cats nearby.",
    "NSLocationWhenInUseUsageDescription": "We use your location to help you find cats nearby."
  }
}

--------------------------------------------------
APP BEHAVIOR
--------------------------------------------------

- Start background tracking automatically after successful login.
- Continue running even if app is minimized.
- Continue running when screen is locked.
- Resume automatically if OS restarts app background task.
- Do NOT rely on UI state.

--------------------------------------------------
ERROR HANDLING
--------------------------------------------------

- If API call fails, log error silently.
- Do not crash app.
- Continue sending location on next interval.

--------------------------------------------------
TESTING INSTRUCTIONS
--------------------------------------------------

- Must be tested on physical Android device.
- Confirm backend receives location every X minutes.
- Confirm it still works after minimizing app.
- Confirm it still works after closing app (Android).

--------------------------------------------------
FINAL STEP
--------------------------------------------------

Ensure app runs:

npx expo start

Do not explain what you are doing.
Implement real production-grade background tracking.