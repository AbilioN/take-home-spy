I am using Node.js v24.

Inside this repository, I want to create a NestJS backend inside a folder named:

abilio-solution/backend

Please do the following:

1) Install NestJS CLI globally if not installed.
2) Create a new NestJS project inside abilio-solution/backend.
3) Configure it to use:
   - TypeORM
   - PostgreSQL
   - class-validator
   - class-transformer

4) Create a basic clean architecture structure with:
   - users module
   - locations module
   - auth module (prepared but simple)

5) Implement:

User entity:
- id (uuid)
- email (unique)
- createdAt

Location entity:
- id (uuid)
- user (ManyToOne relation)
- latitude (float)
- longitude (float)
- createdAt

6) Configure TypeORM with environment variables:
   DB_HOST
   DB_PORT
   DB_USER
   DB_PASS
   DB_NAME

7) Enable automatic entity loading.
8) Enable validation pipes globally.
9) Add CORS configuration.

10) Create a POST /locations endpoint that:
   - Accepts userId, latitude, longitude
   - Validates input
   - Saves location in database

11) Create:
   GET /users/:id/last-location
   GET /users/:id/history

12) Add a docker-compose.yml at project root that:
   - Runs postgres
   - Runs the Nest app
   - Uses proper environment variables

13) Ensure the project builds and runs with:
   docker-compose up --build

Keep the code clean, modular, and production-ready.
Do not generate unnecessary example code.
Follow NestJS best practices.