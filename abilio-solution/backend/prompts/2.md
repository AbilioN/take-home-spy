Now I want to:

1) Install and configure Jest properly.
2) Implement a complete end-to-end (E2E) test suite.
3) Create a separate Admin module with its own table.

Please do the following carefully and cleanly:

------------------------------------------------------
PART 1 — JEST + E2E TESTING SETUP
------------------------------------------------------

1) Install and configure:
   - jest
   - @nestjs/testing
   - supertest
   - ts-jest
   - any required types

2) Configure a dedicated test database using environment variables:
   DB_TEST_HOST
   DB_TEST_PORT
   DB_TEST_USER
   DB_TEST_PASS
   DB_TEST_NAME

3) Ensure tests:
   - Use a separate PostgreSQL database
   - Run with TypeORM synchronize true only in test environment
   - Clean database between tests

4) Create proper E2E tests for:

POST /locations
- Should create a location successfully
- Should fail with invalid payload

GET /users/:id/last-location
- Should return latest location

GET /users/:id/history
- Should return ordered list of locations

5) Ensure tests:
   - Spin up Nest testing module properly
   - Use Supertest
   - Do not mock database
   - Test real integration flow

6) Create an npm script:
   "test:e2e"

------------------------------------------------------
PART 2 — ADMIN MODULE (SEPARATE TABLE)
------------------------------------------------------

Now create a separate Admin module.

Requirements:

1) Create Admin entity with:
   - id (uuid)
   - email (unique)
   - password (hashed)
   - createdAt

2) Admin must NOT reuse User table.
   It must be a separate table.

3) Create:
   - Admin module
   - Admin service
   - Admin controller (basic placeholder)

4) Prepare Admin authentication structure:
   - Password hashing using bcrypt
   - Basic login method returning success response (JWT optional for now)

5) Keep Users and Admins logically separated.

6) Keep architecture clean and modular.

------------------------------------------------------

Follow NestJS best practices.
Keep the code production-ready.
Do not generate unnecessary example or demo code.
Keep everything consistent with existing project structure.